{
  "name": "argvio-node",
  "private": true,
  "description": "Node.js SDKs for emitting CLI usage telemetry to Argvio's ingest server",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/getargvio/argvio-node.git"
  },
  "homepage": "https://github.com/getargvio/argvio-node",
  "bugs": {
    "url": "https://github.com/getargvio/argvio-node/issues"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "pretypecheck": "npm run build -w packages/core",
    "pretest": "npm run build -w packages/core",
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "vitest bench",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8",
    "@typescript-eslint/parser": "^8",
    "@vitest/coverage-v8": "^4.1.11",
    "eslint": "^9",
    "prettier": "^3",
    "tsup": "^8",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
